import styled from "styled-components";

export const StyledSelectedSoundsText = styled.p`
  font-family: ${(props) => props.theme.fontFamily.secondary};
  font-size: 0.8rem;
  color: ${(props) => props.theme.palette.olive};
`;
